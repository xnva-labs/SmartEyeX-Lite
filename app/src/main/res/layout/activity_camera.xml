<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Overlay neon tipis biar futuristic -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#1A00E5FF" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="#80000000">

        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📷 Arahkan kamera ke objek..."
            android:textColor="#00E5FF"
            android:textSize="16sp"
            android:gravity="center"
            android:padding="12dp"
            android:background="#1A00E5FF"
            android:layout_marginBottom="24dp"
            android:fontFamily="monospace" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/backButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:text="◀"
                android:textSize="24sp"
                android:backgroundTint="#FF0266"
                app:icon="@android:drawable/ic_menu_revert"
                android:layout_marginEnd="32dp"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.MaterialComponents.Button.Circle" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/captureButton"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:text="●"
                android:textSize="32sp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#00E5FF"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.MaterialComponents.Button.Circle" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>