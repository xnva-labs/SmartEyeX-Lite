<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context=".CameraActivity">

    <!-- ========== PREVIEW KAMERA (FULL SCREEN) ========== -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ========== SCAN LINE (ANIMASI) ========== -->
    <View
        android:id="@+id/scanLine"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#00FFCC"
        android:alpha="0.6"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- ========== TOP BAR ========== -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Back Button -->
        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_revert"
            android:background="@android:color/transparent"
            android:tint="#FFFFFF"
            android:contentDescription="Kembali" />

        <!-- Title -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="SmartEyeX Lite"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginHorizontal="8dp" />

        <!-- Mood Text -->
        <TextView
            android:id="@+id/moodText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="😌 Tenang"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:paddingEnd="8dp" />

        <!-- Toggle Realtime -->
        <ToggleButton
            android:id="@+id/realtimeToggle"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:textOff="📷 Foto"
            android:textOn="👁️ Siaga"
            android:textSize="11sp"
            android:background="@drawable/toggle_background"
            android:textColor="#FFFFFF" />

    </LinearLayout>

    <!-- ========== RESULT TEXT (TENGAH) ========== -->
    <TextView
        android:id="@+id/resultText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📷 Mode Foto"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:gravity="center"
        android:padding="16dp"
        android:background="#AA000000"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ========== KNOWLEDGE TEXT (ATAS RESULT) ========== -->
    <TextView
        android:id="@+id/knowledgeText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📚 Siap belajar..."
        android:textColor="#AACCDD"
        android:textSize="11sp"
        android:gravity="center"
        android:padding="4dp"
        android:background="#66000000"
        app:layout_constraintTop_toBottomOf="@id/resultText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ========== PROGRESS BAR ========== -->
    <ProgressBar
        android:id="@+id/scanProgress"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:visibility="gone"
        android:indeterminateTint="#00FFCC"
        app:layout_constraintTop_toBottomOf="@id/knowledgeText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- ========== BOTTOM BAR ========== -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:background="#AA000000"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Capture Button (Mode Foto) -->
        <ImageButton
            android:id="@+id/captureButton"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@android:drawable/ic_menu_camera"
            android:background="@drawable/circle_button"
            android:tint="#FFFFFF"
            android:contentDescription="Ambil Foto" />

        <!-- Mic Button (Mode Realtime) -->
        <ImageButton
            android:id="@+id/micButton"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@android:drawable/ic_btn_speak_now"
            android:background="@drawable/circle_button"
            android:tint="#FFFFFF"
            android:visibility="gone"
            android:contentDescription="Bicara" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>